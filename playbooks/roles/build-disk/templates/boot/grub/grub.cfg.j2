set default=0
set timeout=0

menuentry "VyOS AMI (HVM) {{ version_string.stdout }}" {
  linux /boot/{{ version_string.stdout }}/vmlinuz boot=live selinux=0 vyos-union=/boot/{{ version_string.stdout }} console=ttyS0 net.ifnames=0 elevator=noop
  initrd /boot/{{ version_string.stdout }}/initrd.img
}
